# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# =============================================================================
# 大文件与实验资产：走 Release 或对象存储，不进 Git
# =============================================================================
*.pt
*.pth
*.bin
*.safetensors
*.parquet
*.wav
*.mp3
*.flac
/model/
/out/
/dataset/
/release/
/runs/

# 参考 logits fixture 是小型验证夹具，必须入库——它是"移植是否忠实"的唯一判据
!artifacts/reference-logits-*.npz

# 盲听的揭盲表。仓库是公开的，评分员也拿得到，入库就等于把答案发给了被测的人
# ——三份评分表现在还全是空的。评分收齐之后再连同结果一起提交。
artifacts/listening_*/key.json

# =============================================================================
# 工具与缓存
# =============================================================================
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
coverage.xml
.coverage
*.log

# =============================================================================
# 本地 agent 状态：按 PROJECT_RULES §7 不得提交
# =============================================================================
.codex/
.agents/
.claude/
.superpowers/
.codebase-memory/

# =============================================================================
# 环境与凭据
# =============================================================================
.env
.env.*
!.env.example
*.pem
*.key

# =============================================================================
# IDE / OS
# =============================================================================
.idea/
.vscode/
.DS_Store
Thumbs.db

# 判官凭据。本地一份，永不入库——密钥不进源码，也不该进全局环境变量。
configs/judge.local.json
